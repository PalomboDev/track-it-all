generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

model ParcelProvider {
  id   Int    @id @default(autoincrement())
  key  String @unique
  name String
}
